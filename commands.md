# KyroPad — Daily Commands

### 1. Start Server (Terminal 1)
```powershell
cd server
..\venv\Scripts\activate
python main.py
```

---

### 2. Start Mobile App (Terminal 2)
```powershell
cd mobile
npx expo start
```
*(Use `npx expo start -c` if you need to clear the cache)*
